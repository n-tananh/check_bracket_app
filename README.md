**Checking Bracket App** 

This application uses *the **stack** data structure* so that the 
application *checks that the brackets in your code balanced*,
making sure your code does not have any errors about brackets.

> Tony
